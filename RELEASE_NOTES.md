这一版加了一整层新东西：**规则不再只是写给 AI 看的建议，有闸门在它外面拦。**

## 🆕 强制层：AI 绕不过去的那道闸

以前灵台给 AI 的是一套方法论文件——它读了，然后**靠自觉**。
自觉的问题不是 AI 不听话，是**没有任何东西在它没照做时拦一下**。
于是「这条规则从没报过警」和「这条规则根本没接上」，从外面看一模一样。

现在装了五道闸，跑在模型**外面**的进程里，AI 改不了也绕不开：

| 它拦什么 | 拦到会怎样 |
|---|---|
| 回答里没有「我查了什么」的证据行 | 打回去重写 |
| **声称某个东西「已生效 / 正在运行」，而这一轮一次都没去查** | 打回去，让它先查 |
| 要改你的核心文件，而那个目录不在 git 下（改错了没法还原） | 拦下 |
| `rm -rf`、`git reset --hard`、强制推送这类**不可逆**命令 | 拦一次，让它先说清楚"做坏了拿什么还原"，说了就放行 |
| 新开一个窗口 | 不拦，把开工顺序和该先读哪些文件告诉它 |

第二条守的是最常踩的那个坑——**说在跑，其实从没生效过**。我们自己的记录里，同一形状发生过六次。

⚠️ 每一道闸都可以单独关，关掉后**每次回复都会提醒你它是关的**。
静默失效的闸比没有闸更贵：你会以为它在守着。

## 🆕 它会告诉你下一个该建什么闸

灵台的经验库里，每一条坑都带一句「什么情况下这条坑就不可能再发生了」。
那句话其实就是一份**闸门的规格说明书**，只是以前没人把它和「已经建成了什么」对上。

现在一条命令能看到：

- **守护率**：多少条坑真的有东西在守（这个数只因为你**建了一道闸**而涨，不因为你**又记了一条坑**而涨）
- **缺口清单**：按这条坑**实际咬过你几次**排序——把「68 条没守」变成「这条咬过你 6 次，而它的规格早写好了」

## 🔴 修了一个会让你看到报错窗口的问题

双击两次 exe（或者手快点了两下），有几率其中一个窗口报错退出。
原因是两个实例同时写同一份进度文件。已修。

## 怎么启用强制层

装机脚本会自动挂：

```
python -X utf8 install.py
```

只有 exe 没有 Python 的话：

```
lingtaios.exe --install-hooks
```

它**只往你的配置里加，不覆盖**你已有的任何设置；已经挂过就什么都不做；
你的配置文件万一有语法错误，它一个字节都不动并告诉你先修哪里。改之前会留一份备份。

### ⚠️ 这一层需要 Claude Code

闸门用的是 Claude Code 的 hooks 机制。**如果你用的是别的 AI 工具，这一层挂不上**——
灵台的其余部分（项目状态、经验库、方法论文件、继续做指令）不受影响，照常可用。

## 怎么升级

把新的 `lingtaios.exe` 覆盖到你原来那个目录，**其余什么都不用动**。
你记的坑、项目库、配置都在 exe 旁边，不会被碰。

灵台自带的方法论文件：**你没改过的会自动跟着升级，你改过的原样保留**——全程不问你任何问题。

升级完记得跑一次上面那条命令挂强制层，它不会自动挂到你的 AI 配置上。

## 下载

`lingtaios-v0.3.0-win64.zip`，解压到一个固定目录即可，不需要装 Python。
**别解压到 `C:\Program Files` 或「下载」文件夹**——它要在自己旁边写你的配置和记录。

> ⚠️ exe 没有代码签名，Windows 会弹 SmartScreen「已保护你的电脑」。
> 这是未签名程序的通用提示。要继续：点「更多信息」→「仍要运行」。
> 不放心可以从源码自己打包，代码全在仓库里：`python -m PyInstaller lingtaios.spec`。

中文路径、带空格的路径都支持。目前只有 Windows 64 位版。

## 这一版验到什么程度

- 闸门本身 60 条判据，每一条「必须拦住」旁边都配了一条「不许误拦」
- 三个平台（Linux / macOS / Windows）跑同一套判据，全绿
- 整套自检 30 项全过

⛔ 但有一件事没验过：**从 exe 装出来的强制层，只在作者的 Windows 上跑过**。
Linux/macOS 那两个平台验的是源码形态。你要是在别的环境撞到问题，那正是我们想知道的。

## 许可证没变

[FSL-1.1-ALv2](LICENSE)：可读、可审计、可改、可自用、可基于它给客户提供服务；
不可做成与灵台竞争的商业产品对外售卖；每版满 2 年自动转 Apache-2.0。

## 反馈

[Issue](https://github.com/xieyi138-star/lingtaios/issues) ·
[Discussions](https://github.com/xieyi138-star/lingtaios/discussions) ·
support@nexusaistart.com · 微信 nexusaistart
