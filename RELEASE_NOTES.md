**v0.3.0 的说明里有一句是错的**：「强制层需要 Claude Code，用别的 AI 工具挂不上」。
发出去当天就改掉了。这一版只做两件事——把那句话变成真的不成立，以及不让它把你的磁盘越用越满。

用 v0.3.0 的直接覆盖升级；从没装过的直接看下面的"怎么装"。

## 🆕 用 Cursor 也能挂上了

```
lingtaios.exe --install-hooks --host cursor
```

判断规则本身跟你用哪个 AI 工具无关。被锁死的只有两处——**输入字段叫什么**、
**拦下来怎么表达**——现在都收在一份配置文件里，所以接一个新工具是改配置，不是改代码。

| 工具 | 状态 |
|---|---|
| Claude Code | ✅ 实测过，三个平台的自动化测试都跑过 |
| Cursor | ⚠️ **没在真 Cursor 上跑过**——我们手上没有。规则与 Claude Code 那套完全共用（那套已验），接线依据 Cursor 官方文档写成 |
| Windsurf / Cline | 还没接。它们也有同类机制，接法一样 |

⛔ Cursor 那一份装完请自己确认一次：让 AI 说一句不带"我查了什么"的话，看它有没有被打回。
**没拦住就是没接上——而那和"它在守着"从外面看一模一样。**
撞到问题请告诉我们，那正是现在最想知道的事。

## 🔴 它不会把你的磁盘越用越满

强制层每拦一次就记一行流水、每个会话留一个计数文件。
上一版**没有任何东西删它们**——配置里写着"保留 90 天"，而那句话没有任何代码在执行。

现在：流水保留 90 天、会话计数保留 7 天，开新窗口时自动清理，清掉多少也记一笔。

顺带把实际开销测了一遍（上一版文档里那个数字是估的，不是测的）：

- 每次拦截判断约 1.2–1.5 秒
- 100 KB 的长回复不会变慢（0.8 倍）
- 同时开 12 个窗口写流水，一行不丢、一行不坏
- 重度使用（每天 200 次拦截）一年约占 10 MB

## 怎么装

```
python -X utf8 install.py          # 有 Python
lingtaios.exe --install-hooks      # 只有 exe
```

它**只往你的配置里加，不覆盖**你已有的任何设置；已经挂过就什么都不做；
你的配置文件万一有语法错误，它一个字节都不动并告诉你先修哪里。改之前会留一份备份。

## 怎么升级

把新的 `lingtaios.exe` 覆盖到你原来那个目录，其余什么都不用动。
你记的坑、项目库、配置都在 exe 旁边，不会被碰。你没改过的方法论文件会自动跟着升级，改过的原样保留。

## 下载

`lingtaios-v0.3.1-win64.zip`，解压到一个固定目录即可，不需要装 Python。
**别解压到 `C:\Program Files` 或「下载」文件夹**——它要在自己旁边写你的配置和记录。

> ⚠️ exe 没有代码签名，Windows 会弹 SmartScreen「已保护你的电脑」。
> 点「更多信息」→「仍要运行」。不放心可以从源码自己打包：`python -m PyInstaller lingtaios.spec`。

中文路径、带空格的路径都支持。目前只有 Windows 64 位版。

## 这一版验到什么程度

- 闸门 67 条判据，每条「必须拦住」旁边都配了一条「不许误拦」
- 另有 14 条压力测试：长输入、超大对话记录、并发写入、磁盘增长
- 三个平台（Linux / macOS / Windows）跑同一套判据，全绿
- 整套自检 31 项全过

⛔ 仍然没验过的：**从 exe 装出来的强制层，只在我们的 Windows 上跑过**；
Cursor 那一份没有真环境。这两条撞到问题都请告诉我们。

## 许可证没变

[FSL-1.1-ALv2](LICENSE)：可读、可审计、可改、可自用、可基于它给客户提供服务；
不可做成与灵台竞争的商业产品对外售卖；每版满 2 年自动转 Apache-2.0。

## 反馈

[Issue](https://github.com/xieyi138-star/lingtaios/issues) ·
[Discussions](https://github.com/xieyi138-star/lingtaios/discussions) ·
support@nexusaistart.com · 微信 nexusaistart
