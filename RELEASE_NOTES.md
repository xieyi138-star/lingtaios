## ⚠️ 这一版换了许可证，请先看这段

**Apache-2.0 → [FSL-1.1-ALv2](LICENSE)**（Functional Source License，Sentry 提出）。

| | |
|---|---|
| ✅ 你可以 | 自己用、公司内部用、改成你自己的、非商业教育与研究、基于它给客户提供服务 |
| ❌ 你不可以 | 把它做成与灵台竞争的商业产品或服务对外售卖 |
| ⏳ 两年后 | 每个版本发布满 2 年，**自动转为 Apache-2.0** |

源码照样**可读、可审计、可改、可自用** —— 只堵住"改个名字拿去卖"这一个出口。

之前发布的 v0.1.2 仍然是 Apache-2.0，那份授权不会被撤回。

---

## 让任何 AI 按验证过的方法做事

这一版把定位说清楚了：市面上的 AI 记忆工具解决的是**存储**（让 AI 记住你说过什么），
灵台解决的是**方法**（让 AI 按对的方式做事）。

**记忆是你一个人的，方法是所有人共用的。**

### 🌱 新增：坑库回流

"用的人越多方法越准"这句话，之前没有兑现机制 —— 你记的坑回不到主库。现在有了：

- 记完一条坑，界面当场问你：**换个项目、换套技术栈，这条还成立吗？**
- 成立就点「🌱 贡献回主库」，自动带着「坑 / 防法 / 失效判据 / 出处」开一个 Issue
- 审核通过进主库，下一版所有人的 AI 都按更准的方法做事

**它不发任何网络请求、不上传任何数据** —— 只是替你把内容填进 GitHub 的表单，
发不发你自己按。零遥测这条承诺不会因为"想收集贡献"就打折。

入库门槛和你自己记坑一样硬：**没有「失效判据」不收**。判据是让一条坑能退休的唯一机制，
否则坑库只增不减，最后没人看。

## 界面：只留你真会用的

按"用户会据此做什么"逐页清了一遍，答不上来的都从主路径撤走：

- **首页**那个最大的环，从「真源在位 59/59」换成「**已装系统 3/21**」
  —— 真源是灵台自己的内部检查，你不需要知道那是什么
- **坑库**从 7 列摊平改成只列「**坑 + 怎么防**」，点一行才展开出处和失效判据
  —— 那几列写的是记录者自己项目的代号，别人看不懂
- **设置**从 4 个 tab 删到 2 个（整理 / 换机与我的文件）
  - 删掉「开工四问」：实测它只判断有没有填，内容一个字不看，不落盘，切个 tab 就全空
  - 删掉「方法」：那四篇是**给 AI 读的文件**，不是给人在网页里翻的；
    但没有藏起来 —— 在「换机 / 我的文件」里写清楚文件在哪，还能一键打开目录
- 灵台自己的体检（真源在位 / 断头双份 / 通用件落后）全部收进「🔧 体系自检」折叠区
- 首页新增「这东西帮到你了？说一声 →」—— 零遥测意味着我永远不知道有没有人在用，
  这是唯一诚实的办法：给一个你愿意才点的出口

顺带：`data.json` 从 111.4 KB 瘦到 57.8 KB（不再把四篇方法论渲染成 HTML 白传一遍）。

## 下载

`lingtaios-v0.2.0-win64.zip`（约 8.7 MB）解压到任意目录即可，不需要装 Python。
**别解压到 `C:\Program Files` 或系统临时目录** —— 它要在自己旁边写配置和记忆。

> ⚠️ exe 没有代码签名，Windows 会弹 SmartScreen「已保护你的电脑」。
> 这是未签名程序的通用提示。要继续：点「更多信息」→「仍要运行」。
> 不放心可以从源码自己打包，代码全在仓库里：`python -m PyInstaller lingtaios.spec`。

中文路径、带空格的路径都支持（有专门的回归在守）。

## 反馈

[Issue](https://github.com/xieyi138-star/lingtaios/issues) ·
[Discussions](https://github.com/xieyi138-star/lingtaios/discussions) ·
support@nexusaistart.com · 微信 nexusaistart
