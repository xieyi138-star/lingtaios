## 这一版修的是「换台电脑就用不了」

v0.1.1 之前的版本，从 GitHub clone 下来装不上、下载的包里带着开发者本机的路径。
这一版把这条路径从头到尾走通了，并且**每一步都写成了回归测试**，以后不靠人记得。

### 下载

`lingtaios-v0.1.2-win64.zip`（8.6 MB）解压到任意目录即可，不需要装 Python。
**别解压到 `C:\Program Files` 或桌面临时目录**——它会在自己旁边写配置和记忆。

> ⚠️ exe 没有代码签名，Windows 会弹 SmartScreen「已保护你的电脑」。
> 这是未签名程序的通用提示。要继续：点「更多信息」→「仍要运行」。
> 不放心可以从源码自己打包，代码全在仓库里，`python -m PyInstaller lingtaios.spec`。

### 修了什么

**打包态会静默丢用户数据（最严重）**
之前 exe 把 PyInstaller 的临时解压目录当数据目录：你在界面上点「记坑」，接口返回成功、
条数也涨了，**但重启后那条坑就没了**，且没有任何提示。这直接违反「记忆归你」这条承诺。
现在方法论真源首跑落到 exe 旁边的持久目录，读写都走那份；升级 exe 不会覆盖你改过的内容。

**装不上**
`install.py` 假设了开发者的目录布局，从公开仓 clone 下来跑，12 项真源全报 `[XX]`、退出码 1。
`dashboard.py` 源码态、`lingtaios.spec` 打包配方、整套回归脚本都有同样的问题。
现在四个入口统一从运行时位置自推导，主源码布局和发布仓布局都认。

**新用户建的第一个项目在项目库里看不见**
如果你的机器上没有开发者那套业务根目录（绝大多数人都没有），只能选「自定义落点」，
而自定义落点建出来的项目，发现逻辑不认——项目真的建好了、六器官也落盘了，列表里就是空的。
现在创建完会显式登记进项目库。相关的报错也不再叫你去跑 `install.py`（下载 exe 的人根本没有源码）。

**发布包里带着开发者的绝对路径**
随 exe 落到你电脑上的「AI 开窗必读」卡片里，写着 `C:\Users\<开发者>\...`，
你的 AI 照着去找一个不存在的文件。现在卡片里零绝对路径，真实路径一律由程序运行时生成。

### 内容更新

- **坑库 40 → 69 条**：把散在各项目里的跨项目通用坑归并进来（判定/协作/状态/运行时/工具五类），
  每条都带「防法」和「失效判据」——判据是让坑能退休的机制，不是摆设。
- 全部 69 条的失效判据补齐（此前 100% 是「待补」，坑库只增不减）。

### 已知限制

- 只有 Windows 64 位版
- exe 未做代码签名，会触发 SmartScreen
- 升级 exe 时，你改过的方法论文件不会被自动更新（保护你的内容优先），差异需要手动同步

### 反馈

[Issue](https://github.com/xieyi138-star/lingtaios/issues) · support@nexusaistart.com · 微信 nexusaistart
