这一版全是修，没有新功能。三个是 v0.2.0 里真会咬到你的问题。

## 🔴 进度刷不出来（只用 exe 的人一定会撞）

v0.2.0 里，**建项目时会自动生成状态，之后就没有任何办法重算了**。
界面上没有入口，而文档告诉你「跑 `python -X utf8 状态生成器.py`」——
可灵台的卖点就是零依赖，下载 exe 的人多半根本没装 Python。

结果是：你干完一段活，回到驾驶舱，进度还停在建项目那天，而且不报错。

现在有两条路，都不需要 Python：

- 项目详情页 → **🔄 重算状态**
- 命令行 `lingtaios.exe --regen <项目目录>`

「继续做」指令和收窗模板里的命令也跟着改了：**按你实际用的形态给**，不再默认你有 Python。

## 🔴 页面偶发 `Failed to fetch`

HTTP 服务的连接队列用了 Python 标准库的默认值 **5**。Windows 在这个队列排满时会直接
发 RST 掐断连接，浏览器里就显示 `TypeError: Failed to fetch`——看起来就像服务挂了。
多开几个标签页、或者 AI 在轮询的时候就够触发。

隔离实测（同样 200 请求 / 50 线程，跑 8 轮，只改这一个值）：

| 队列长度 | 结果 |
|---|---|
| 5（v0.2.0） | 1588/1600，8 轮里 4 轮有失败 |
| 128（本版） | 1600/1600，零失败 |

## 🔴 首页「本周 +77」是假的

那 77 条是**出厂自带**的，带的是作者的入库日期。你刚装完什么都没干，
首页就说你这周新增了 77 条。现在改成相对出厂快照算——装完没干活就是 0，
你自己记一条才 +1。

一个满口「先实证再开口」的东西，首屏摆个编出来的数字，比少一个数字贵得多。

## 🟠 状态是哪天算的，现在写在脸上

项目卡上原本永远是一句「✓ 状态已生成」——**一年前生成的和刚生成的长得一模一样**。
现在写清楚「✓ 状态 今天 / ⏳ 状态 12 天前」，超过 7 天变黄，详情页还会直说
「已经 N 天没重算了，上面的数字可能是旧的」。

这条不是锦上添花。作者自己的项目就栽在这儿：状态停在一天前，写着「坑库 38 条 ✅ 无告警」，
而真实条数已经 80——**驾驶舱顶着一份旧快照报平安，漂了 42 条没有任何东西出声**。
补了重算按钮还不够，没人告诉你该按，按钮等于不存在。

## 🟠 出厂文件终于能随版本升级了

以前落到你磁盘上的方法论文件是「不存在才复制」——**新版带的更新永远不会生效**。
不是偷懒，是当时分不清哪份是你改的、哪份是我们上次写下去的，分不清就只能一律不动。

现在有台账了，于是：

| 情况 | 行为 |
|---|---|
| 你没动过 | **自动升级**到新版 |
| 你改过 | **绝不覆盖**，留着你的 |

**你什么都不用做，也不会被问任何问题。** 灵台记着每一版发出去时文件的原样，
一比就知道你动没动过它——包括 v0.2.0 装的那批。

⛔ 「不覆盖你改过的东西」这条不给任何开关。有专门的回归在守这几条分支。

## 其他

- **压缩包里现在有一层目录**了（`lingtaios-v0.2.1\`）。v0.2.0 是 5 个文件平铺在包根，
  在「下载」文件夹里点「全部解压」，它们会散进下载夹，然后 exe 就把你的项目库和
  经验库写在下载夹里，跟一堆安装包混在一起。
- **一个项目都没有时**，首页会告诉你下一步，不再只显示一个 0。
- 刚建好的项目，详情页不再摆一屏 `<...>` 占位符冒充内容，改成告诉你下一步。
- 「这东西帮到你了？说一声」挪到你**真的跑起来一个项目之后**才出现——
  一个字没用过就被问「帮到你了吗」，既尴尬，答案也没有信息量。
- **演练模式**（`--roots-file`）此前仍会往装配图里写登记行，现在一并收进演练目录。
- 经验库 74 → **80** 条。
- README 里那个还没建好的官网链接摘掉了。

## 怎么升级

把新的 `lingtaios.exe` 覆盖到你原来那个目录里，**其余什么都不用动**。
你记的坑、项目库、配置都在 exe 旁边，不会被碰。

就这样，没有第二步。你没改过的文件会自动跟着升级，你改过的原样留着——
不会弹任何东西问你。

## 下载

`lingtaios-v0.2.1-win64.zip`（约 8.7 MB）解压到任意固定目录即可，不需要装 Python。
**别解压到 `C:\Program Files` 或系统临时目录** —— 它要在自己旁边写配置和记忆。

> ⚠️ exe 没有代码签名，Windows 会弹 SmartScreen「已保护你的电脑」。
> 这是未签名程序的通用提示。要继续：点「更多信息」→「仍要运行」。
> 不放心可以从源码自己打包，代码全在仓库里：`python -m PyInstaller lingtaios.spec`。

中文路径、带空格的路径都支持（有专门的回归在守）。

## 许可证没变

[FSL-1.1-ALv2](LICENSE)：可读、可审计、可改、可自用、可基于它给客户提供服务；
不可做成与灵台竞争的商业产品对外售卖；每个版本发布满 2 年自动转 Apache-2.0。

## 反馈

[Issue](https://github.com/xieyi138-star/lingtaios/issues) ·
[Discussions](https://github.com/xieyi138-star/lingtaios/discussions) ·
support@nexusaistart.com · 微信 nexusaistart
