在用 v0.2.0 的建议升级：这一版修了三个会真咬到你的问题。没有新功能。

## 🔴 进度刷不出来

v0.2.0 里，项目建好之后就**没有任何办法重算进度**了——你干完一段活回到驾驶舱，
它还停在建项目那天，而且不报错。

现在：项目详情页点 **🔄 重算状态**。不需要装 Python。

## 🔴 页面偶尔报 `Failed to fetch`

多开几个标签页、或者 AI 正在轮询的时候，页面可能突然报错，看起来像整个挂了。已修。

## 🔴 首页那个「本周 +N」是假的

它把**出厂自带**的经验条数算成了你本周的新增。现在装完什么都没干就是 0，
你自己记一条才 +1。

## 顺手改好的

- 项目卡写清楚状态是哪天算的（「今天」/「12 天前」），超过 7 天变黄提醒你重算
- 压缩包里多了一层目录——以前在「下载」文件夹里点解压会散一地
- 刚建好的项目，详情页不再摆一屏 `<...>` 占位符，改成告诉你下一步
- 一个项目都没有时，首页告诉你下一步，不再只显示一个 0
- 经验库里某一条不用了，点开那一行就能让它退休

## 怎么升级

把新的 `lingtaios.exe` 覆盖到你原来那个目录里，**其余什么都不用动**。
你记的坑、项目库、配置都在 exe 旁边，不会被碰。

灵台自带的方法论文件：**你没改过的会自动跟着升级，你改过的原样保留**——
全程不会问你任何问题。

## 下载

`lingtaios-v0.2.1-win64.zip`（约 8.7 MB），解压到一个固定目录即可，不需要装 Python。
**别解压到 `C:\Program Files` 或「下载」文件夹**——它要在自己旁边写你的配置和记录。

> ⚠️ exe 没有代码签名，Windows 会弹 SmartScreen「已保护你的电脑」。
> 这是未签名程序的通用提示。要继续：点「更多信息」→「仍要运行」。
> 不放心可以从源码自己打包，代码全在仓库里：`python -m PyInstaller lingtaios.spec`。

中文路径、带空格的路径都支持。目前只有 Windows 64 位版。

## 许可证没变

[FSL-1.1-ALv2](LICENSE)：可读、可审计、可改、可自用、可基于它给客户提供服务；
不可做成与灵台竞争的商业产品对外售卖；每版满 2 年自动转 Apache-2.0。

## 反馈

[Issue](https://github.com/xieyi138-star/lingtaios/issues) ·
[Discussions](https://github.com/xieyi138-star/lingtaios/discussions) ·
support@nexusaistart.com · 微信 nexusaistart
