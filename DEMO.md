# Demo 剧本（60-90 秒 · 从一句话到交付物）

> 目标：一条视频讲清"任何 AI 装进来，立刻变成会实证、有记忆、状态可见的智能体"。
> 素材全部真实操作录屏，不用动画。

## 分镜

### 0:00-0:10 痛点（10 秒）
录屏：一个 AI 对话窗，问"这个项目做到哪了"，AI 回复"我无法访问你之前的会话，请提供背景"。
字幕：**"每次换窗口，AI 都失忆。"**

### 0:10-0:25 双击（15 秒）
双击 `lingtaios.exe` → 浏览器打开 → 首页：已装系统 N/M 的环 + 告警/项目数/经验库 + 项目卡。
字幕：**"双击，你的 AI 大脑全在。"**

### 0:25-0:45 继续做（20 秒）
点开一个项目卡 → 显示：进行中的注、上一窗做完的/没做完的、最近轨迹。
点「复制继续做指令」→ 粘贴到 AI 窗口 → AI 第一段回复（证据头+真读施工图+实查数据库）。
字幕：**"任何 AI，三句话接上进度——不用考古。"**

### 0:45-0:65 越用越强（20 秒）
坑库页：一屏「坑 + 怎么防」→ 点「＋ 记一条坑」录一条 →
录完当场弹「换个项目、换套技术栈还成立吗？」→ 点「🌱 贡献回主库」。
设置「整理」：过期的、该退休的自动浮出，勾选删掉。
字幕：**"踩一次坑记一条，通用的还能回流——用的人越多，这套方法越准。"**

### 0:65-0:85 换电脑/换模型（20 秒）
展示：同一个项目，把指令粘给另一个完全不同的 AI → 它同样接上。
字幕：**"模型随便换，记忆不丢。记忆是你的资产，不是哪个 AI 的。"**

### 0:85-0:90 收尾（5 秒）
GitHub 地址 + 一句话定位。
字幕：**"让任何 AI 按验证过的方法做事——而且用的人越多，这套方法越准。"**

## 录制清单

- [ ] 干净虚拟机或新用户目录（证明零依赖）
- [ ] 双语字幕版本各一（中文字幕主版，英文副版）
- [ ] 全程真实操作，不加速不改脚本输出

## 首发帖钩子（即刻/V2EX 各一版，草稿）

> ⛔ 发之前对一遍：**坑库条数、版本号、许可证**这三样别写死在草稿里——
> 上一版草稿就把「38 条」和「Apache-2.0」发成了过期信息。发帖前去界面上看实时数。

**即刻版**（钩子=痛点）：
> 用了 6 个月 AI，做了 20 多个项目，最大的痛点不是模型不够强，是**换个窗口就失忆**、教训烂在聊天记录里。所以我把半年踩的坑做成了一个本地系统：双击打开，点一下复制一段话，粘给任何 AI，它就知道这个项目做到哪、该干什么、什么不许做。<br>每条坑都带「失效判据」——防的事被结构性解决了就自动浮出来等你删，不是只增不减的清单。<br>零遥测、全本地、代码可读。[图：截图×3]

**V2EX 版**（钩子=技术）：
> 分享一个自用的「个人 Agent OS」：不是给 AI 做记忆存储，是给它装一套**验证过的做事方法**——证据头协议（先实证再开口）、带失效判据的坑库、机器生成的项目状态（生成器自带破坏性自检，绿了才信）。<br>零依赖（只用 stdlib + 手写 md 渲染器），模型无关（任何 AI 读文件即接入），零遥测全本地。<br>许可证是 FSL-1.1-ALv2：源码可读可改可自用，只禁止拿去做竞品，**每版满 2 年自动转 Apache-2.0**。<br>已在 20 多个真实项目上跑了半年。[链接+架构图]

**共用的一句话定位**：
> 让任何 AI 按验证过的方法做事，而不是按它猜的——**而且用的人越多，这套方法越准**。
