# 5 分钟上手

## 第 1 分钟 · 打开

双击 `lingtaios.exe`（或 `python -X utf8 dashboard.py`）→ 浏览器自动打开 `http://127.0.0.1:8765`。

首页三个大数字：真源在位 / 告警项目 / 项目总数。全绿 = 开工。

## 第 2 分钟 · 建一个项目

点「＋ 新项目」→ 填：
- 项目名（随便）
- 落点（Nexus 根 / D 盘 / 自定义）
- 终极之果（至少 1 行：这个项目做到什么算成）
- 红线（绝不做的事）

点「创建」→ 六器官自动装好、状态自动生成、装配图自动登记。全程零手动文件操作。

## 第 3 分钟 · 开工

点开项目卡 → 点「📋 复制『继续做』指令」→ 打开任意 AI 粘贴。

AI 会：读图纸 → 读交接 → 告诉你现在到哪了、下一步是什么 → 每完成一段自动写进度。

## 第 4 分钟 · 看进度

任何时候双击 exe：
- 首页：红绿灯 + 该生核的提醒
- 项目卡：进行中的注 + 做完的/没做完的 + 最近轨迹
- 不用记项目在哪——卡片就是入口，一键打开目录

## 第 5 分钟 · 让它生长

- 踩了坑 → 坑库页「＋ 记一条坑」（防法/失效判据必填）
- 每周 → 进化页跑一次审计：到期/失效/过期全浮出，勾选删除
- 体系升级了 → 项目卡「⚠ 可升级」一键同步

## 换电脑

1. 拷走整个目录（或 clone 仓库）
2. 跑 `python -X utf8 install.py`（探测各根）
3. 跑 `python -X utf8 dashboard.py`

业务数据（本机专属）留原机；方法体系全带走。
