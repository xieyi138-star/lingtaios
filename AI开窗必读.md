# AI 开窗必读 · 任何 AI 接入本体系的入口

> 本卡与具体产品无关（Claude Code / Cowork / 任何支持读文件的 AI 均适用）。
> 把它当作新同事的入职手册：**照做开窗五步，你就是这个体系的协作者。**

## 开窗五步（照做，不要跳）

1. **读本卡**（当前）
2. **读装配图** → `C:\Users\Administrator\.claude\skills\project-delivery\装配图.md`（唯一导航真源：查任何知识先查图再读文件，禁满盘 grep）
3. **读规则** → 同目录 `常驻薄核.md`（每条消息都在跑的规则）+ 项目 `brain\01_法典.md`（有的话）
4. **拿真值** → 跑 `python -X utf8 状态生成器.py` 或读 `02_状态.md`——人写的完成度表默认当过期处理
5. **打证据头** → 每段回复第一行：`【证据】<工具> <路径> → <真实输出>`；没查过就标 `【无证据】` 并只说推测

## 分段落盘（代替"等收窗"）

> **创始人随时会离开，进度必须一直在文件里，不依赖谁记得发收窗指令。**

每完成一个完整任务段（一个注/一个修复/一个验证），**立刻**把「做到哪 / 下一步」更新进项目交接文件（`brain\05_交接.md`；没装系统的项目写 `HANDOFF.md`）。不等窗口结束、不等指令——这是你的默认习惯，不是仪式。

## 轨迹落盘（自进化的原料）

每完成一段，同时把「做了什么 / 证据 / 结果」追加一行到 `brain\traces\YYYY-MM-DD.jsonl`（目录不存在就建），一行 JSON：
`{"t": "时间", "act": "动作", "ev": "证据", "res": "结果"}`。
交接文件给人看"到哪了"，轨迹给机器看"怎么做的"——后者是未来自动提炼技能/教训的原料。

## 协作底线（三条永不豁免，全文见常驻薄核）

- 实证先于实操：关于当前状态的说法，先用工具读出来再说
- 每步说得出服务终极之果的哪一条，说不出就不做
- 每件事有「做完了」的判据，且不能是过程量

## 本机的体系全景（人可看）

浏览器打开灵台（双击桌面「灵台」，或 `python -X utf8 C:\Users\Administrator\.claude\skills\brain-console\dashboard.py`）：真源红绿灯 / 项目卡片 / 坑库 38 条 / 方法四真源 / 装机说明。
