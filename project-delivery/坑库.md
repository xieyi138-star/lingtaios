# 坑库 · 跨项目 B 坑唯一归一处

> **本文件是唯一真源。** 进坑、查坑都走这里，不许在别处再抄一份。
> 项目专属坑不进本库，各自留在自己项目里（本机落点见装配图 L3，用 `{NEXUS}` / `{D}` 别名登记，
> 别在这里写死绝对路径——这份文件会随发布仓公开，写死等于把作者的磁盘布局发出去，
> 对读者还毫无意义）。
> 条目纪律：每条必须带**防法（照做即可）**+**失效判据（防的事被结构性消除即删）**；涉密（IP/密钥/客户数据）不进本库。
> 触发次数：同形坑再触发就 +1，不新增条目。

> **2026-08-16 归并**：此前跨项目通用的坑大量散在两个项目库里、灵台查不到——
> 实测 Nexus 归档 81 条、Savant 25 条，而本库只有 40 条。已把其中**跨项目通用**的
> 并进来：Nexus §7.2/§7.3/§7.4/§7.5 → 12 条，Savant → 16 条，另 7 条同形只 +1 触发
> （见各条「触发」列的 ＋ 标记）。归并当时本库从 40 条涨到 68 条，之后随手记的另算——
> **总数不写在这里**，看灵台界面上的实时数，写死了就是又一处要人同步的副本（见 P10）。
> **留在原处的是平台专属坑**（OpenWrt / vnstat / MT6000 / efuse / Xray / OpenClash 等约 55 条，
> 以及 Savant 的流水线内部坑）——它们进来只会稀释本库，查它们走装配图 L3 登记的落点。
> 判断边界只问一句：**换一个项目、换一套技术栈，这条还成立吗？** 成立才进本库。

## 判定侧

| 编号 | 一句话坑 | 防法（照做即可） | 出处 | 触发 | 失效判据 | 入库 |
|---|---|---|---|---|
| P1 | 量具自身的错系统性偏向「更合格」——绿灯比红灯更该怀疑 | 读数为绿时先查量具本身，再高兴 | Savant 2026-08 | 4 个量具同日全错 ＋Savant 坑3/坑16 | 每个量具都带「喂坏样本必报红」自检且已进回归 | 2026-08-15 |
| P2 | 空内容让每条判据都沉默，而沉默被读成合格 | 跑判据之前先查空，查空先于跑判据 | Savant（DRY-RUN 桩满分） | 1 | 判据引擎遇空集强制报错，结构上产生不了 PASS | 2026-08-15 |
| P3 | 批量作业不先跑 N=1，同根因会重复 N 次 | 批量前先跑最小一个验通再放大 | Savant（12 节点同一根因×12） | 1 | 批量入口强制先过 N=1，未过不放大 | 2026-08-15 |
| P4 | 探针被出生样本拟合，真复发读数 0 而它一直全绿 | 验收必须喂 git 里的历史真案现场 | Savant | 4 次真复发漏检 | 验收集固定含 git 历史真案，新探针须先在真案上报红 | 2026-08-15 |
| P5 | 测量前环境不在稳态，读出反向结论 | 量之前先让环境进稳态 | Savant（缓存冷启动） | 1 | 稳态检查内建进量具，未达稳态拒绝出数 | 2026-08-15 |
| P6 | 整体红 ≠ 每一个都红；外推和实测在文档里长得一样 | 断言「每个都」之前逐格实测 | Savant（6 格实 1 格） | 1 | 报告格式强制逐格列出，不接受只给聚合值 | 2026-08-15 |
| P7 | 破坏性验证的「破坏」没真构成变化，断言当场假红 | 先证明破坏真构成变化（内容逐字对比）再验 | Savant | 1 | 破坏性测试框架强制先断言破坏前后 sha 不同 | 2026-08-15 |
| P8 | 「读数是 0」和「读数只能是 0」长得一样 | 接尺之前先证明它读得出别的值 | Savant | 1 | 量具接入前必过「能读出非零值」的正向用例 | 2026-08-15 |
| P9 | 盲测＝还没确认两组走不走同一条路就开跑 | 不确定先查全网最优解，不猜不盲测 | Savant | 1 | 对比实验前强制记录并断言两组执行路径一致 | 2026-08-15 |
| P10 | 同一判据抄多份必分叉（判据逐字节抄了 28 份） | 数字/判据只有一份真源，其余从它取 | stock-agent 陷阱7 | 1 ＋Savant 坑15 | 该判据/数字只有一处定义，其余由它生成，无人工副本 | 2026-08-15 |
| P11 | 静默兜底=伪自动化：出不了货必须硬停出声 | 兜底被用上时必须有人知道 | stock-agent 陷阱9（卖出日静默夹尾） | 1 ＋Savant 坑11 | 兜底分支强制产生可见信号，静态检查证明无静默 return | 2026-08-15 |
| P12 | 同一段 hold-out 看太多次就不再是 hold-out | 「调参后变好」落盘前换一个从没看过的切分重跑，判据先写死 | stock-agent 陷阱12 | 1 | hold-out 使用次数被工具记录，超 1 次拒绝出结论 | 2026-08-15 |
| P13 | 对照组的样本量也要够——8 个样本足以得出方向相反的结论 | 对照组样本与结论强度同步加 | stock-agent 三·23 | 1 | 样本量不足时判据模板自动拒绝出结论 | 2026-08-15 |
| P14 | 「被验证过能跑」的结论在验证条件变了之后不成立 | 验证条件变了，结论重新验 | stock-agent（cs_rank OOM） | 1 | 验证结论带条件指纹（环境/数据/版本），指纹变即自动失效 | 2026-08-15 |
| P15 | 头条数字是多次抽样的最大值，不是中心值 | 引用好看数字前先问：中心值是多少？换等价变体重跑一次看掉多少 | stock-agent 三·13/16 | 5 变体散布 15pp | 报告模板强制给中心值与散布，不接受单点数字 | 2026-08-15 |
| P16 | 无数据支撑的假设固化进决策链，下游全污染（"IPRoyal 不可换 IP"错算 ¥322 → 整个经济模型污染） | 任何写入决策文档的数字必须有来源；无来源标「待验证」不进下游 | NEXUS archive_cases 2.1 | 1 | 决策文档每个数字强制带来源字段，无来源被工具拦下 | 2026-08-15 |
| P17 | 没有整体图就施工，连续诊断失败 4 次 | 先有图再施工：决策前先建整体思路与架构对齐 | NEXUS vnstat 修复 2026-05-13 | 4 | 「整体图已存在」是进入施工的硬关口，未过不放行 | 2026-08-15 |
| P18 | 人写的「还没跑」不算证据，先看文件系统 | 断言某流程没执行过之前，先读文件系统/日志的真值 | stock-agent 陷阱13 | 1 | 「是否执行过」一律由文件系统/日志查询回答，流程无人工字段 | 2026-08-15 |
| P19 | 破坏点挑错会把「闸是哑的」误读成「判据没过」；文本模式回写 LF→CRLF，只有 sha 才查得出 | 破坏点读源码挑真抛分支；还原按字节验（sha256 对比） | Savant destructive-test-pick-the-throw-site | 1 | 破坏性测试按 sha256 验还原，破坏点由源码抛出分支自动定位 | 2026-08-15 |
| P20 | 模拟未来状态时 3 条命中里 2 条是自造噪声（供体是产线自己的闸会拒绝的） | 模拟只许模拟「可达状态」，先证明供体真实可达 | Savant simulate-only-reachable-states | 1 | 模拟框架强制先校验供体在产线闸下真实可达 | 2026-08-15 |
| P21 | 架构没做完就开跑，术:法比 15.5:1 跑不出来是必然 | 架构做完才测试，不许按科目推 | Savant architecture-first-test-last | 1 | 「架构已完成」是进入测试的前置关口，未过不放行 | 2026-08-15 |
| P22 | 判定侧拿错键（`kind` 不存在时回落拿错尺） | 判定必须用约定键名，键不存在不许兜底 | Savant kind-vs-knowledge-type | 2（同日两次） ＋Savant 坑7（一天三次） | 取键一律走约定常量，键不存在直接抛错，代码中无 fallback 回落 | 2026-08-15 |
| P23 | 在开发目录里验可移植性——全绿也证明不了别人能用，因为写死的路径和 dirname 层级假设在自己机器上碰巧都是对的 | 交付物必须真 clone/复制一份到无关目录，按用户文档第一条命令跑，看退出码 | 灵台 OS 2026-08-16（主源码 selftest 24/24、回归 16 项全绿，clone 出来 install.py 12 项全红退出码 1） | 1 | clone 冒烟（装→跑→打包→自检）已进回归，未过不许发布 | 2026-08-16 |
| P24 | 纸面设计稿 ≠ 工程实现：写设计稿和真写代码是两个认知模式，设计稿模式会系统性省略工程师本能（严格模式/落盘实证/输入校验全缺） | 设计稿写完先按工程标准逐项自检；且纸面自检抓不全，必须真跑边界用例才算验过 | Nexus §7.5.1+§7.5.6（设计稿 vs update_progress.sh 逐项对比，7 个维度全缺） | 1 | 工具类交付的验收清单里含「跑过边界用例」且未跑不放行 | 2026-08-16 |
| P25 | 逐个修下游小坑而不看根因层：做对一个基础设施会自动消掉多个下游坑，先修小坑就是无用功 | 修之前先问这坑的真根因在哪一层；根因层能一次性覆盖多个下游的，先做根因层 | Nexus §7.5.5（部署 PowerShell $PROFILE UTF-8 后 §7.2.5 行数误算坑自动消失） | 1 | 候选补丁池按「真根因层」分组，同根因的合并成一条基础设施任务 | 2026-08-16 |
| P26 | 权威文献/白皮书的证据不可尽信：12 支柱证据库 8/12 条 citation 系编造，伪造指纹是未来日期+精确数字+以期刊名当作者 | 引用前逐条查真文献；查不到就删，不留「大意如此」 | Savant 坑1（2026-07-25） | 1 | 引文有机器可查的校验（DOI/可解析链接），查不到不允许写入 | 2026-08-16 |
| P27 | 读「质量分数」之前没查污染通道：答案顺着七类 74 个内联点漏进来，校验器的眼睛也一起被糊住 | 读数前先枚举污染通道并逐个堵；校验器的输入必须与被测对象隔离 | Savant 坑2+坑9（黄金注入通道七类 74 个内联点） | 1 | 污染通道枚举进回归，新增通道会让回归转红 | 2026-08-16 |
| P28 | 尺子不分学科就是在扭曲内容：一把按某一学科标定的判据，换个学科就在量别的东西，而它照样出分 | 判据标定时写死适用学科；换学科必须重标定，不许直接复用 | Savant 坑6+坑24（判据按一个学科标定，换学科即失效） | 1 | 判据带适用范围元数据，超范围调用直接拒绝而不是出分 | 2026-08-16 |
| P29 | 一套模板套所有知识类型；且分流只改了生成侧没改判定侧，于是判定侧继续按老模板打分 | 按类型分流；分流的每一次修改必须同时落到生成侧和判定侧两边 | Savant 坑8 | 1 | 生成侧与判定侧的类型表同源导出，改一处另一处自动跟随 | 2026-08-16 |
| P30 | 判据对定性科目是瞎的——它给的「通过」根本不含信息，却和定量科目的通过长得一样 | 定性科目单独立判据；瞎的判据必须显式返回「不适用」而不是「通过」 | Savant 坑10 | 1 | 判据对不适用输入返回 N/A 且聚合时不计入通过率 | 2026-08-16 |
| P31 | 回归量的是代理指标：平时和真指标同涨同跌，**恰恰在最需要它的时候**给绿灯 | 回归至少有一条直接量终极结果的用例，代理指标只做补充 | Savant 坑12 | 1 | 回归里含端到端真指标用例，且它红了代理指标绿也算红 | 2026-08-16 |
| P32 | 判据用形容词写（「清晰」「合理」「充分」），于是每次判的都是判官的心情 | 判据一律写成可观察行为：谁、在什么输入下、能看到什么具体输出 | Savant 坑17 | 1 | 判据模板强制「可观察行为」字段，形容词式判据创建不出来 | 2026-08-16 |
| P33 | 稳定性 ≠ 有效性：先把稳定性调绿了，结果量的是一个无效的东西——稳定地错 | 先测有效性再测稳定性，顺序不许反 | Savant 坑18 | 1 | 流程强制有效性用例先过，未过不允许进稳定性阶段 | 2026-08-16 |
| P34 | 判据放错层：本该在单元级拦的缺陷放到节点级去判，于是一个缺陷变成 N 次节点级失败 | 判据放在缺陷产生的那一层；下游只做汇总，不做首次判定 | Savant 坑19（2026-07-27） | 1 | 判据登记时写明所在层，跨层判定在评审时被拦 | 2026-08-16 |
| P35 | 尺子没在量——出来的分数其实是硬闸结论的转写，闸怎么判分就怎么给，量具本身没工作 | 接尺前先证明它能读出与闸不同的值；分数与闸结论完全同步就是没在量 | Savant 坑20（2026-07-27 立，同日重写并处置） | 1 | 量具接入前必过「读数与闸结论解耦」的正向用例 | 2026-08-16 |
| P36 | 地板和天花板是同一个数，中间没有量程——所有样本都贴边，看起来很稳其实什么都没区分 | 定阈值前先看分布：地板≈天花板就先扩量程，别急着调阈值 | Savant 坑21 | 1 | 阈值设定前强制出分布图，量程不足直接拒绝设阈值 | 2026-08-16 |
| P37 | 换量表会**静默**重定义所有按比例引用它的阈值：改了上下界，所有写成百分比的闸全变了，而没有一条报错 | 阈值一律存绝对值或显式绑定量表版本；换量表必须列出受影响的全部引用点 | Savant 坑22（2026-07-27） | 1 | 阈值带量表版本号，量表变更时引用点未更新会直接报错 | 2026-08-16 |
| P38 | 论据和测试用了两套例子：论证里举的那个例子从头到尾没被测过，于是那句话从未被检验 | 论据里举的例子必须就是测试用的例子，一一对应 | Savant 坑25（2026-07-27） | 1 | 文档里的示例与测试用例同源，不一致会被工具检出 | 2026-08-16 |

## 协作与派工

> 细则唯一真源 = `agent-worksheet` 技能（三特性 / 工单必含 / 探针 / 回炉循环），本库不复制，只登记三件血案：

| 编号 | 一句话坑 | 防法（照做即可） | 出处 | 触发 | 失效判据 | 入库 |
|---|---|---|---|---|
| W1 | 修错 Bug——看到的错误 ≠ 用户说的错误 | 先复现用户说的那个 Bug，别看到异常就改 | NEXUS（往返 N 次一下午） | 1 | 工单模板强制含用户报告的复现步骤，验收须重跑它 PASS | 2026-08-15 |
| W2 | 产出去向不明＝白干 | 落点绝对路径钉死 + 回执即证据 | agent-worksheet | — | 工单模板强制绝对路径落点+回执，缺一不能派工 | 2026-08-15 |
| W3 | 派出去的活没验收＝空气 | 绝不凭「它说做完了」，按判据验产出 | agent-worksheet | — | 验收判据是工单必填项，无判据的工单创建不出来 | 2026-08-15 |
| W4 | AI 连续工作 6 小时以上，实证习惯会间歇性衰减——内化过的规则也不免疫，新假设会架在上一个错误诊断上 | ≥6 小时后每个写操作前强制自问「上一段实证过现状吗」；人看到 AI 直接给命令没先实证时当场打断 | Nexus §7.4.2+§7.4.6（一夜 9 次诊断被推翻；铁律已部署仍被当场抓出跳实证） | 1 | 写操作前的实证自检由工具强制（不靠 AI 自觉），未实证不允许执行 | 2026-08-16 |
| W5 | 新对话的 AI 继承不了工作节奏：文档记了「做了什么」却没记「怎么做」，于是新窗口直接改真源/给增量补丁让人手动整合 | 交接必须含节奏层（怎么做、先读什么、什么不许做），不能只有进度 | Nexus §7.4.3（换新对话后 AI 直接在生产改文件） | 1 | 开窗指令由系统按真实路径生成且含节奏层，人不用手写交接 | 2026-08-16 |
| W6 | 注入层（Custom Instructions / 全局指令）写了不等于生效——没显式点名的章节会被跳读 | 注入层改完必须端到端实测新会话的真实行为，不能假设写了就生效 | Nexus §7.4.5+§7.5.4（旧版只说「读 §1-3」，§1.6/§1.7 被跳读） | 1 | 注入层变更有配套的行为验收用例，未过不算部署完 | 2026-08-16 |
| W7 | 凭印象生成「数字事实」：Unicode 码位/版本号/参数值这类查得到的东西，AI 会拼一个出来，连续错 4 次（连记录这个坑时都再错一次） | 数字事实一律现查（如 Python print(hex(ord('字')))）；能用字面量就别用转义 | Nexus §7.4.7（写「瞄」字：瞑→瀪→瞄，元层再错 1 次） | 1 | 写入前对数字事实做机器校验（查表/断言），不靠人眼复核 | 2026-08-16 |
| W8 | 多条线并行改同一个仓库，每条线都以为自己是唯一那条，互相覆盖且谁都没察觉 | 并行前先约定各自的文件边界；共同文件走串行或显式合并 | Savant 坑23 | 1 | 并行工作在隔离工作区里进行，合并时冲突会显式报出 | 2026-08-16 |

## 状态与文档

| 编号 | 一句话坑 | 防法（照做即可） | 出处 | 触发 | 失效判据 | 入库 |
|---|---|---|---|---|
| S1 | 凭滞后快照推断「有问题」，一头扎进真实环境刨 | 开窗先看最近实际记录，再导航 | NEXUS INDEX 血案 | 1 ＋Nexus §7.4.4/§7.5.2 | 状态机器生成且带时戳，超期自动标记为过期、不可被引用 | 2026-08-15 |
| S2 | 工具显示「成功」≠ 写进去了 | 改完必 Read 回读；跨机走 md5 | NEXUS 假成功血案 | 1 ＋Savant 坑5、Nexus §7.3.1/§7.3.2 | 写操作封装强制回读校验，代码中不存在裸写接口 | 2026-08-15 |
| S3 | 声称在服务的功能实际从未生效（六起同形） | 声称验证必须用会列全量的工具实查（如 xray api lsrules），grep 补查 | NEXUS G-claim | 6 ＋Savant 坑13（一天六次） | 「正在生效」由列全量的接口实查，流程中无人工声称字段 | 2026-08-15 |
| S4 | 写死的 ground truth 过期，屏幕还在报旧数 | 同一数字只存一处（唯一常量），换模型必重跑并更新它 | stock-agent 术三血案 | 1 | 该数字只有一处定义且由生成器产出，无人工同步步骤 | 2026-08-15 |
| S5 | 「归档」只是把东西挪到没人看得见的地方 | 归档前确认目标仍在版本控制与回归视野内，且登记入装配图 | 项目交付法 §7 | — | 归档动作强制登记装配图，且归档物仍在回归视野内 | 2026-08-15 |
| S6 | 反思成果不当下落盘：想着「先做完手头活，反思晚点再说」，战略洞察一晚上就没了 | 反思发现的缺陷当下写进待办池/坑库，不等收窗 | Nexus §7.5.3（创始人原话：反思的成果要及时落盘，不能后面再犯） | 1 | 每段收尾有「这段学到什么」的必填字段，空着不允许标记完成——自动落轨迹只记「做了什么」，捕获不到洞察 | 2026-08-16 |
| S7 | 记了债不修，等于没记：待办池越记越长，记录本身变成了「已处理」的错觉 | 记债时同时定处置时限与判据；到期未处置要么做要么显式销账 | Savant 坑4 | 1 | 待办池条目带到期日，过期项自动浮到进化审计里 | 2026-08-16 |
| S8 | 写入登记的是 A，消费方读的是 B——东西真的建好了、文件也在磁盘上，界面上就是看不见 | 登记完立刻用**消费方的那条读路径**回查一次；两条路径不是同一条，就是 bug 不是配置问题 | 灵台 OS 2026-08-16（新项目登记进装配图 L6，而项目发现只认 {D}/ 开头的行，自定义落点建的项目建出来却不显示；六器官 12 个文件都在磁盘上） | 1 | 写入与消费共用同一条读路径，或写入后有回查断言，回查不到即报错 | 2026-08-16 |

## 运行时与系统

| 编号 | 一句话坑 | 防法（照做即可） | 出处 | 触发 | 失效判据 | 入库 |
|---|---|---|---|---|
| R1 | 凭名字判「孤儿」就删——实则有硬编码 rule 正指着它 | 删生产长驻前：列全量规则 + grep 配置文件补查 | NEXUS NODE03 断网 | 1（差点废几个月） | 删生产长驻前强制产出「引用全量列举+配置 grep」报告，否则执行不了 | 2026-08-15 |
| R2 | 生产长驻进程 pkill -9 | 动生产进程前先想回滚 | NEXUS | 1 | 停生产进程走带回滚点的封装命令，裸 pkill 被禁用 | 2026-08-15 |
| R3 | 数据落 C 盘——曾到 93% | 数据目录上数据盘（D:）+ 目录联接 | stock-agent | 1 | 数据目录由配置统一指向数据盘，且有容量告警 | 2026-08-15 |
| R4 | 打包态把 sys._MEIPASS 当数据目录——写得进、接口返 200、界面数字也涨，进程一退出临时目录被清理，用户数据静默消失 | 打包态区分「程序资源」(随包只读) 与「用户资产」(落到 exe 旁边持久目录)，写操作一律指向后者 | 灵台 OS 2026-08-16（记一条坑 rows 38→39，重启回 38） | 1 | 回归里有「写入→重启进程→复查仍在」的持久化用例，只验返回码不算 | 2026-08-16 |
| R5 | 一个开关三套值域：越界值不是脏数据，它是探测器——说明有一路调用方在用你不知道的语义 | 看到越界值先去追是谁写的，不要直接清洗掉；值域必须唯一且有校验 | Savant 坑14 | 1 | 该开关的值域收敛为唯一一套且入口有校验，越界直接拒绝 | 2026-08-16 |
| R6 | 「有回滚点」只算了版本库，没算工作区里未提交的改动——重写历史 / reset --hard 类操作会把它们一并清掉，且无处可恢复 | 不可逆操作前先跑一次 status：有未提交改动就先 stash 或整份复制出去，确认工作区干净再动手；备份 .git 不等于备份了工作区 | 灵台 OS 2026-08-16（git filter-repo 清历史前只备份了 .git，工作区那版未提交的录屏被 reset 掉，全盘搜不到副本） | 1 | 危险操作前的检查清单里含「工作区无未提交改动」，未满足直接拒绝执行 | 2026-08-16 |
| R7 | 默认值是开发者机器的假设：`root_choice` 默认 nexus、路径默认 C:\某某——在作者机器上永远是对的，别人一用就撞 | 默认值只能来自「这台机器的实况」或干脆不给默认（缺参数就报错，并列出本机可用的选项） | 灵台 OS 2026-08-16（新用户没有 NEXUS 根，建项目直接被拒，错误信息还叫他去跑一个他根本没有的 install.py） | 1 | 所有默认值要么运行时探测，要么强制显式传参；代码里搜不到硬编码的环境默认值 | 2026-08-16 |

## 工具坑

| 编号 | 一句话坑 | 防法（照做即可） | 出处 | 触发 | 失效判据 | 入库 |
|---|---|---|---|---|
| T1 | PowerShell 命令行写中文会被吃掉/卡续行符；`>` 重定向是 UTF-16LE | 命令行不写中文；写文件用专用工具 | 全局（用户级 CLAUDE.md） | 多 | 命令行调用不再经 PowerShell 传参（换 shell 或走封装） | 2026-08-15 |
| T2 | UTF-8 BOM 破坏 `^` 锚定正则，计数静默少算 | 跑 Python 脚本用 `python -X utf8`；正则输出加自检 | skills 仓库（E2E 抓到） | 1 | Python 入口固定 -X utf8，读文本走统一封装，无裸 open | 2026-08-15 |
| T3 | create_file 写进容器沙箱，Windows 侧找不到 | Windows 路径写文件用 write_file | 全局（用户级 CLAUDE.md） | 1 | 工具集里不再有 create_file，或其行为已改为写 Windows 侧 | 2026-08-15 |
| T4 | 审计仪器自身会失败——4 连错而代码每次都对 | 严格拒绝式接口会把审计者自己的错变成「被审计者的证据」；仪器输出先当嫌疑再当证据 | freight-audit | 4 | 仪器自检能区分「仪器自己错」与「被审计者错」（错误分类字段） | 2026-08-15 |
| T5 | 看到的错误≠要修的错误，改了别处就说修好了 | 修完重跑用户报告的那个复现，PASS 才算 | NEXUS | 1 | 修复验收强制重跑用户报告的那个复现，PASS 才算完 | 2026-08-15 |
| T6 | 显示层丢字 ≠ 文件层丢字：终端渲染出来缺字，文件其实是完整的——反过来照着「修」会把对的改坏 | 唯一可信源是把文件重新读出来（Get-Content/read），不看终端回显 | Nexus §7.2.3（28+ 次实证：PSReadLine 显示丢字而文件完整） | 1 | 终端渲染与文件内容的一致性由工具保证，或流程里根本不看回显判断 | 2026-08-16 |
| T7 | PowerShell 编码默认值是坑窝：5.1 默认 GB2312，Out-File 自动加 BOM，Get-Content 不带 -Encoding 会把中文错分行导致行数算错 | $PROFILE 里设 $PSDefaultParameterValues['*:Encoding']='utf8'；脚本里显式带 -Encoding | Nexus §7.2.1+§7.2.4+§7.2.5（sync 工具报 1145 行，实际 1332 行） | 1 | 所有 PowerShell 入口的默认编码已在 $PROFILE 统一为 utf8 且有自检 | 2026-08-16 |
| T8 | PSReadLine 渲染大段中文 here-string 会崩：≥15 行就丢字/闪烁 | 每段 here-string ≤15 行；避免三反引号；宁可分多次贴 | Nexus §7.2.2（28+ 次显示丢字实证） | 1 | 改用不经 PSReadLine 的输入通道（文件/管道/其它 shell） | 2026-08-16 |
| T9 | 多层嵌套转义不可靠：PowerShell here-string 套 ssh 套 bash 套 $? 时，$? 会被外层提前展开，bash 收到的不是字面量 | 不做 4 层嵌套——先登进目标机进 bash 提示符，再贴纯命令 | Nexus §7.2.6（退出码捕获拿到 True/False 而非 0/1） | 1 | 跨机执行走脚本文件传输而非嵌套字符串拼接 | 2026-08-16 |
| T10 | 上传/同步后的 UI 暂态 ≠ 真实状态：显示两份同名文件，此时删除或重传会真的搞坏 | 看到异常暂态先等 30-60 秒重开再看；用「能不能搜到新内容」验真，不看列表 | Nexus §7.3.3（Project Knowledge 替换上传后 UI 短暂双份） | 1 | 上传接口返回可查询的版本号/指纹，凭它判生效，不看 UI 列表 | 2026-08-16 |

## 失效判据约定

条目里「失效判据」若为结构性条款（如 T1 随 PowerShell 习惯改掉而失效），在装配图改版时整行删除并记录。

**判据必须可判定**——写成一个能查真假的结构性状态，不能写「以后注意」。
「以后注意」不是判据是愿望，用它当判据这个循环在结构上就永远关不掉
（常驻薄核第三条：完工判据不能是过程量）。

> ⚠️ **2026-08-16：这 38 条的失效判据是 AI 依据每条坑的成因推导补齐的，不是创始人原话，需逐条复核。**
> 此前 38 条全是「待补」——闸装在了入口（进坑向导缺判据直接 400），存量却一条没过闸，
> 于是坑库只增不减。补齐是为了让「删」这个动作有依据，不是为了把格子填满：
> **判据写错了比空着更坏**，因为它会让人误以为某条坑已经可以退休。
> 复核时只问一句：**这个状态成立时，这条坑还可能发生吗？** 还可能，就是判据写松了。
