# 交接

> 器官⑤ 记忆。**每次收窗更新。** 交接的是**方法的连续，不是某个实例的人格**。

---

## 给下一个人 / 下一个窗口的三件套

1. **方法底座** → `01_法典.md`（薄核）+ 技能 `project-delivery`
2. **项目准则** → `00_宪法.md`（果与红线）+ `01_法典.md` 的「项目专属」段
3. **当前状态与下一步** → 下面

---

## 现在到哪了

⛔ **不要在这里手写完成度数字。** 读 `02_状态.md`（机器生成）。
手写的状态写完当天就可能过期，而且不会报错。

要重算，挑一条你机器上跑得通的：
- 驾驶舱 → 项目页 → **🔄 重算状态**（不用命令行，最稳）
- `lingtaios.exe --regen <项目目录>`
- 源码态：`python -X utf8 状态生成器.py`（⛔ 只有装了 Python 才有这条）

**最近一次生成**：<日期>　**有无告警**：<有/无>

---

## 这一注在做什么

见 `03_在建.md`。一句话：<...>

---

## 下一个人开工前必须知道的

| # | 事 | 为什么它重要 |
|---|---|---|
| 1 | | |

> 只写「不知道会踩坑」的。项目背景、历史沿革一律不写——那是 C 类，进不了交接。

---

## 本窗做完的 / 没做完的

**做完**：<...>
**没做完且为什么**：<...>
**熔断了吗**：<到期没做完的注，说明当初整形错了，别默认延期>

---

## 开窗五步（照做，不要跳）

```
0. 打证据头（第一个动作是读真实状态，不是回忆）
1. 读 01_法典.md
2. 跑状态生成器 → 拿真值
3. 对齐最近记录（人写的状态是滞后快照，不是真值源）
4. 观象一句话：时位 + 向果 —— 两者都不是 → 停
```
